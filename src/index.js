/** @format */

import { division, multiplication, subtraction, sum } from "./calc.js";

const resultSum = sum(1, 2);
const resultSubtraction = subtraction(1, 2);
const resultMultiplication = multiplication(1, 2);
const resultDivision = division(1, 2);
